name: Container CI

on:
  pull_request:
    paths:
      - "orphanet_link/**"
      - "server.py"
      - "mcp_server.py"
      - "docker/**"
      - "scripts/**"
      - "pyproject.toml"
      - "uv.lock"
      - "container-release.json"
      - ".dockerignore"
      - ".env.docker.example"
      - "CHANGELOG.md"
      - ".github/workflows/container-ci.yml"
      - ".github/workflows/container-release.yml"
  push:
    branches:
      - main
    paths:
      - "orphanet_link/**"
      - "server.py"
      - "mcp_server.py"
      - "docker/**"
      - "scripts/**"
      - "pyproject.toml"
      - "uv.lock"
      - "container-release.json"
      - ".dockerignore"
      - ".env.docker.example"
      - "CHANGELOG.md"
      - ".github/workflows/container-ci.yml"
      - ".github/workflows/container-release.yml"

permissions: {}

jobs:
  container-ci:
    permissions:
      contents: read
    uses: berntpopp/genefoundry-router/.github/workflows/_container-ci.yml@e0a330acc0b8d0deb72de35a27345e5c3351de74 # genefoundry-router container-release standard v1
